{
  "name": "cloud-mail-intake",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "cloud-mail": "scripts/cli.mjs"
  },
  "scripts": {
    "cloud-mail": "node scripts/cli.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "tsc --noEmit",
    "test": "node --experimental-strip-types --test tests/**/*.test.ts",
    "setup": "node scripts/setup.mjs",
    "setup:domain": "node scripts/setup-domain.mjs",
    "install:global": "node scripts/install-global.mjs"
  },
  "dependencies": {
    "postal-mime": "^2.6.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260606.0",
    "@types/node": "^24.10.3",
    "typescript": "^5.9.3",
    "wrangler": "^4.82.2"
  }
}
